import { as } from 'rdf-namespaces';

export const LeaveMessage = as.Leave;

export const actor = as.actor;
export const object = as.object;
export const profile = 'http://purl.org/solidelight/terms/profile';
