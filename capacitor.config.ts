import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.stieger.solidelight',
  appName: 'Solidelight',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
